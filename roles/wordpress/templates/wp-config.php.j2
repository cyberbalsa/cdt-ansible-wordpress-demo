<?php
/**
 * WordPress Configuration File
 *
 * This file was generated by Ansible!
 * Notice how the database credentials are filled in from variables.
 *
 * @package WordPress
 */

// =============================================================================
// DATABASE SETTINGS - These are filled in by Ansible from our variables
// =============================================================================

// The name of the database
define( 'DB_NAME', '{{ wordpress_db_name }}' );

// Database username
define( 'DB_USER', '{{ wordpress_db_user }}' );

// Database password
define( 'DB_PASSWORD', '{{ wordpress_db_password }}' );

// Database server - this points to our MySQL server!
define( 'DB_HOST', '{{ database_server }}' );

// Database charset
define( 'DB_CHARSET', 'utf8mb4' );

// Database collation (leave blank for default)
define( 'DB_COLLATE', '' );

// =============================================================================
// AUTHENTICATION KEYS AND SALTS
// =============================================================================
// These are security keys - in production you'd generate unique ones
// Visit https://api.wordpress.org/secret-key/1.1/salt/ to generate

define( 'AUTH_KEY',         'ansible-demo-key-1' );
define( 'SECURE_AUTH_KEY',  'ansible-demo-key-2' );
define( 'LOGGED_IN_KEY',    'ansible-demo-key-3' );
define( 'NONCE_KEY',        'ansible-demo-key-4' );
define( 'AUTH_SALT',        'ansible-demo-salt-1' );
define( 'SECURE_AUTH_SALT', 'ansible-demo-salt-2' );
define( 'LOGGED_IN_SALT',   'ansible-demo-salt-3' );
define( 'NONCE_SALT',       'ansible-demo-salt-4' );

// =============================================================================
// DATABASE TABLE PREFIX
// =============================================================================

$table_prefix = 'wp_';

// =============================================================================
// DEBUGGING
// =============================================================================

define( 'WP_DEBUG', false );

// =============================================================================
// ABSOLUTE PATH - Don't edit below this line
// =============================================================================

if ( ! defined( 'ABSPATH' ) ) {
    define( 'ABSPATH', __DIR__ . '/' );
}

require_once ABSPATH . 'wp-settings.php';
